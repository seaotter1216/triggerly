spring:
  datasource:
    url: jdbc:mysql://localhost:3306/triggerly
    username: triggerly
    password: triggerly
  elasticsearch:
    uris: http://localhost:9200
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092
